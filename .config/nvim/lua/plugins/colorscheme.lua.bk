local M = {
	"rose-pine/neovim",
	lazy = false,
	priority = 1000,
	opts = function()
			return {
				variant = "moon",
			}
			end,
		config = function(_, opts)
		local rosepine = require("rose-pine")
		rosepine.setup(opts)
		-- rosepine.load()
	end,
}

return M
